import * as ldk from "lightningdevkit";

const createInvoice = async (amtMsat, description) => {
    // Create an invoice for the specified amount and description
    // console.log(ldk)
    // Need to find appropriate method for creating invoice
    // const invoice = await ldk.Invoice.createInvoice({
    //   value: 10000,
    //   description: "coffee"
    // });
    // const encodedInvoice = invoice.to_str();
    // return encodedInvoice;
}

export default createInvoice;